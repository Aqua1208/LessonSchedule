<% content_for :title do %>
  <h1>Editing lesson</h1>
<% end %>

<%= render "form", lesson: @lesson %>

<div>
  <%= form_with model: @lesson, method: :delete, data: { confirm: '本当に削除しますか？' } do |f| %>
    <%= f.submit "講座削除", class: 'btn btn-danger', disable_with: 'Submitting...' %>
  <% end %>
</div>
