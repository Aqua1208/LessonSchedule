<% content_for :title do %>
  <h1>Users</h1>
<% end %>

<% content_for :nav do %>
<% end %>

<%= render "user" %>

<div id="users">
  <table class="table table-striped table-hover">
    <tr>
      <th>#</th>
      <th>利用者名</th>
      <th></th>
    </tr>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.name %></td>
        <td><%= link_to "詳細", user %></td>
      </tr>
    <% end %>
  </table>
</div>

<%= button_to "New user", new_user_path, method: :get, class: "btn btn-teal" %>
