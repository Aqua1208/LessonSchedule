<nav class="admin">
  <h1>Users</h1>
  <div>
    <%= button_to "SignOut", signout_path, method: :delete %>
    <%= button_to "Lessons", lessons_path, method: :get %>
  </div>
</nav>
<p style="color: green"><%= notice %></p>

<div id="users">
  <table>
    <tr>
      <th>#</th>
      <th>利用者名</th>
      <th></th>
    </tr>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.user_name %></td>
        <td><%= link_to "詳細", user %></td>
      </tr>
    <% end %>
  </table>
</div>

<%= link_to "New user", new_user_path %>
