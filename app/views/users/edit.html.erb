<% content_for :title do %>
  <h1>Editing User</h1>
<% end %>

<% content_for :nav do %>
<% end %>

<%= render "form", user: @user %>

<br>

<div class="flex">
  <%= button_to "詳細", @user, method: :get, class: "btn btn-teal" %>
  <%= button_to "削除", @user, method: :delete, class: "btn btn-danger" %>
  <% if current_user.admin %>
    <%= button_to "一覧", users_path, method: :get, class: "admin btn btn-teal" %>
  <% end %>
</div>
