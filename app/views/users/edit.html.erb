<% content_for :title do %>
  <h1>Editing user</h1>
<% end %>

<% content_for :nav do %>
<% end %>

<%= render "form", user: @user %>

<br>

<div>
  <%= button_to "詳細", @user, method: :get, class: "btn bg-teal" %>
  <%= button_to "削除", @user, method: :delete, class: "btn bg-teal", data: { confirm: "Are you sure you want to delete this user?" } %>
  <% if current_user.admin %>
    <%= button_to "一覧", users_path, method: :get, class: "admin btn bg-teal" %>
  <% end %>
</div>
